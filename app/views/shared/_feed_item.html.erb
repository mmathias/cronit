<li id="<%= feed_item.id %>">
  <span class="content"><%= feed_item.id %> </span>
  <span class="content"><%= feed_item.start_date %> </span>
  <span class="content"><%= feed_item.how_long %> </span>
  <span class="content"><%= feed_item.how_often %> </span>
  <span class="content"><%= feed_item.actions %> </span>
  <span class="content"><%= feed_item.calendar_items %> </span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(feed_item.created_at) %> ago.
  </span>
  <% if current_user?(feed_item.user) %>
    <%= link_to "delete", feed_item, method: :delete,data: {confirm: "You sure?"}, title: feed_item.id %>
  <% end %>
</li>